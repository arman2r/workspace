import { CheckHorizontalScrollDirective } from './check-horizontal-scroll.directive';

describe('CheckHorizontalScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckHorizontalScrollDirective();
    expect(directive).toBeTruthy();
  });
});
