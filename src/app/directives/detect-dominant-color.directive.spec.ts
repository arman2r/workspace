import { DetectDominantColorDirective } from './detect-dominant-color.directive';

describe('DetectDominantColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectDominantColorDirective();
    expect(directive).toBeTruthy();
  });
});
